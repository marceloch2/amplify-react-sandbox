type Award @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  AwardTodos: [Todo] @manyToMany(relationName: "AwardTodo")
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  awards: [Award] @manyToMany(relationName: "AwardTodo")
}
 